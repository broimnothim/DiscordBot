import path from 'path';

export const ADMIN_ROLE_ID = '1463505059332489319';

/** Path to config.json (for read/write). Run bot from project root. */
export const CONFIG_PATH = path.join(process.cwd(), 'src', 'config', 'config.json');
